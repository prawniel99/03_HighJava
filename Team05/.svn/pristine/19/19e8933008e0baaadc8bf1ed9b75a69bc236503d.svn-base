<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="maincate">

	<insert id="insertMainCate" parameterType="String">
		INSERT INTO category (cate_id, cate_name)
		VALUES ('C' || TO_CHAR(category_seq.NEXTVAL, 'FM000000'), #{cate_name})
	</insert>

	<delete id="deleteMainCate" parameterType="String">
		delete from category where cate_id = #{cate_id}
	</delete>
	
	<update id="updateMainCate" parameterType="String">
		update category set cate_name = #{cate_name}
		where cate_id = #{cate_id}
	</update>
	
	<select id="getAllMainCate" resultType="maincateVo">
		select * from category order by cate_id desc
	</select>

</mapper>


