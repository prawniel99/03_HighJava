<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jdbc">

	<select id="getMemIdCount">
		select count(*) cnt from mymember where mem_id = #{mem_id}
	</select>

	<insert id="insertMember" parameterType="memVo">
		insert into mymember(mem_id, mem_pass, mem_name, mem_tel, mem_addr)
		values((select nvl(max(mem_id)+1,0) from mymember), #{mem_pass}, #{mem_name}, #{mem_tel}, #{mem_addr})
	</insert>
	
	<delete id="deleteMember" parameterType="kr.or.ddit.vo.MemberVO">
		delete from mymember
		where mem_id = #{mem_id}
	</delete>	
	
	<update id="updateMember" parameterType="kr.or.ddit.vo.MemberVO">
		update mymember
		set mem_name = #{mem_name}, mem_pass = #{mem_pass}, mem_tel = #{mem_tel}, mem_addr = #{mem_addr}
		where mem_id = #{mem_id}
	</update>
	
	<select id="displayMember" resultType="MemberVO">
		select * from mymember where mem_id = #{mem_id}
	</select>
	
	
	
	
	

</mapper>